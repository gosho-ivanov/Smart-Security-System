Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
PyMySQL==1.1.1
Werkzeug==3.0.3
python-dotenv==1.0.1
cryptography==42.0.8

opencv-python-headless  # USB camera MJPEG streaming
face_recognition        # face detection + recognition (requires dlib — see install note below)

# Raspberry Pi hardware — install only on the Pi
# RPi.GPIO       # matrix keypad (4x3)
# pyfingerprint  # AS608 fingerprint sensor
